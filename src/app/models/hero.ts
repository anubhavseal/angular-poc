import { HttpResponse } from "@angular/common/http";

export class Hero {
    id: number;
    name: string;
}

export class Post{
    userId: string;
    id: number;
    title: string;
    body: string;
}
